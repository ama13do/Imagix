import 'package:flutter/material.dart';

class DicomExplorerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Explorer Screen - Coming Soon'),
    );
  }
}